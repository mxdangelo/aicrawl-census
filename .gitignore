# --- Data: heavy raw layer (exceeds GitHub's 100 MB limit; re-derivable) ---
# census.db is the full source layer (raw bodies/headers, ~320 MB).
# Everything downstream is rebuilt from it, and the shippable snapshot is
# census-lite.db (tracked). Keep the heavy DB local.
census.db

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# --- Local tooling / editor ---
.claude/settings.local.json
.vscode/
.idea/

# --- OS junk ---
.DS_Store
Thumbs.db

# --- Legacy static-report layer (kept local as reference) ---
# Superseded by the data-driven (Altair/Quarto) dashboard. The hand-drawn SVG
# generator describes one snapshot and doesn't scale to periodic injections;
# only its validated palette will be salvaged into the dashboard theme.
run_report.py
run_figures.py
report.html
figures/
slides.md

# --- Personal (kept local, never published) ---
# Homework artifacts from the course this study started as — out of place on a
# public observatory. Kept on disk, never tracked.
STUDY-GUIDE.md
presentazione_homerwork_1_dangelo.pptx

# --- Dashboard: local preview render (throwaway) ---
dashboard/_preview.html

# --- Quarto build artifacts ---
dashboard/_site/
dashboard/.quarto/
/.quarto/
.jupyter_cache/

# --- Working artifacts ---
*.backup.*
*.tmp
